# Legacy Modes Marketplace

`marketplace.yaml` is retained for legacy Kilo 5.x clients only. It is not actively maintained.

Kilo is moving from modes to agents. Current clients should use the [agents marketplace](../agents/) instead.
